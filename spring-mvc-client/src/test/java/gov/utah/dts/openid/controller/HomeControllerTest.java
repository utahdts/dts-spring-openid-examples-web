package gov.utah.dts.openid.controller;


public class HomeControllerTest {

/*
		@Test
		public void testCreate() {
			RestTemplate rest = new RestTemplate();

			HttpMessageConverter httpMessageConverter = new FormHttpMessageConverter();
			HttpMessageConverter stringHttpMessageConverter = new StringHttpMessageConverter();
			HttpMessageConverter jsonHttpMessageConverter = new MappingJacksonHttpMessageConverter();
			List<HttpMessageConverter<?>> list = new LinkedList<HttpMessageConverter<?>>();

			list.add(httpMessageConverter);
			list.add(stringHttpMessageConverter);
			list.add(jsonHttpMessageConverter);

			rest.setMessageConverters(list);
			
			Member member = new Member();
			member.setName("foo");
			HttpEntity<Member> entity = new HttpEntity<Member>(member);
			String result = rest.postForObject("http://localhost:8080/rest-training/member.json", entity, String.class);
			
			System.out.println(result);

		}
		*/
		
	}
